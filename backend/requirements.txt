# Werco ERP - Backend Dependencies (v1.0.1)
fastapi==0.136.3
# starlette pinned explicitly: >=1.0.1 fixes CVE-2026-48710 / GHSA-86qp-5c8j-p5mr
# (missing Host-header validation poisons request.url.path, bypassing path-based
# security checks). fastapi>=0.133 dropped the starlette<1.0.0 cap. See
# docs/SECURITY_ADVISORY_SUPPRESSIONS.md.
starlette==1.2.1
uvicorn[standard]==0.40.0
gunicorn==25.0.3  # Production WSGI server
sqlalchemy==2.0.46
alembic==1.18.3
psycopg2-binary==2.9.11
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.3.0
python-multipart==0.0.27  # 0.0.27 fixes CVE-2026-42561
python-dotenv==1.2.2
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.3.0
boto3==1.42.44

# Security
slowapi==0.1.9
bleach==6.3.0  # HTML sanitization for input validation

# WebSocket Support
websockets==16.0

# Caching (optional but recommended)
redis==5.3.1
hiredis==3.3.0

# Monitoring (optional)
sentry-sdk==2.52.0

# Document Processing
pypdf==6.10.2
pdf2image==1.17.0
pytesseract==0.3.13
python-docx==1.2.0

# LLM Integration
anthropic==0.79.0

# Fuzzy Matching
rapidfuzz==3.14.3

# DXF Parsing (existing)
ezdxf==1.4.3

# Background Jobs & Queue
arq>=0.27.0
redis>=5.0,<6.0
croniter>=6.0.0

# Email
jinja2>=3.1.6
aiosmtplib>=5.1.0

# Webhooks (httpx already exists above, adding missing deps)
cryptography>=46.0.4

# Data Export
openpyxl>=3.1.5
xlrd>=2.0.2
pandas>=3.0.0
reportlab>=4.4.9
